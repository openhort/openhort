export default {
  vault: {
    'state': { live: true }
  },

  simulate(ctx) {
    // Llming Lens card is a static mockup — no dynamic simulation needed.
    // The card shows a miniature IDE/Browser/Terminal layout.
  }
}
