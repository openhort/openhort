"""Linux window management extension for openhort.

Manages windows inside a Docker container running X11 (Xvfb + fluxbox).
All operations (list, capture, input, workspaces) execute via docker exec.
"""
